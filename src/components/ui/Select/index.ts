import Root from "./Root";
import Trigger from "./Trigger";
import Content from "./Content";
import Option from "./Option";

export const Select = {
    Root,
    Trigger,
    Content,
    Option,
};

export default Select;
export { Root, Trigger, Content, Option };
