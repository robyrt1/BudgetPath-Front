import Root from "./Input/Root";
import Label from "./Input/Label";
import Field from "./Input/Field";
import Error from "./Input/Error";
import Icon from "./Input/Icon";

export const Input = {
    Root,
    Label,
    Field,
    Error,
    Icon,
};

export default Input;
export { Root, Label, Field, Error, Icon };
